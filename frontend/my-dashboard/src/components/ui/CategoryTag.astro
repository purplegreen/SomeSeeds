---
interface Props {
  label: string
  href?: string
}

const { label, href } = Astro.props
---

{href
  ? <a class="category-tag" href={href}>{label}</a>
  : <span class="category-tag">{label}</span>}
