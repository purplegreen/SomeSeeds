import simplePage from './simple_page'
import navigation from './navigation'
import category from './category'
import tag from './tag'

export const schemaTypes = [simplePage, navigation, category, tag]
